table:
  schema: public
  name: users
remote_relationships:
  - definition:
      remote_field:
        balance:
          arguments:
            where:
              user_id:
                _eq: $id
      hasura_fields:
        - id
      remote_schema: payment
    name: user_balance
  - definition:
      to_source:
        relationship_type: object
        source: payment
        table:
          schema: public
          name: balance_d
        field_mapping:
          id: user_id
    name: user_balance_d
